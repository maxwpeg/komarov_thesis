# Подробный технический отчет по проекту

Дата среза: 2026-04-22

## 1. Что это за проект

Это веб-система проектирования пожарной сигнализации и связанных подсистем на основе поэтажных планов.
Проект объединяет несколько контуров:

- управление карточками проектов;
- хранение и редактирование поэтажных планов;
- ручное и полуавтоматическое размещение элементов на плане;
- пошаговый pipeline распознавания стен, проемов, помещений и зон;
- проектирование пожарных извещателей, приборов, кабельных трасс и СОУЭ;
- каталог оборудования и привязку оборудования к проекту;
- генерацию проектной документации в PDF;
- сбор обучающих примеров и дообучение моделей распознавания.

По сути это не просто CRUD-приложение, а инженерная система, в которой совмещены:

- web-интерфейс редактора;
- backend с прикладной логикой и API;
- компьютерное зрение;
- геометрические алгоритмы;
- PDF-генерация;
- контур машинного обучения для retraining.

## 2. Общая структура репозитория

Ключевые каталоги и их роль:

- `backend/` — основной FastAPI backend, ORM, use cases, routers, сервисы, бизнес-логика.
- `floorplan-ui/` — React frontend.
- `floorplan/` — отдельный Python-пакет с алгоритмами классического распознавания планов.
- `tests/` — backend API/integration тесты.
- `floorplan/tests/` — тесты модуля распознавания.
- `alembic/` — миграции БД.
- `docs/` — документация.
- `uploads/` — загруженные пользователем файлы планов и изображений.
- `outputs/` — генерируемые результаты, PDF, датасеты retraining, артефакты обучения.
- `debug_output/` — отладочные изображения и промежуточные результаты распознавания.
- `floor_plans.db` — SQLite-база по умолчанию.

Отдельно важно отметить root-level Python-файлы:

- `Project.py`
- `Page.py`
- `DrawingPage.py`
- `TitlePage.py`
- `GeneralDataPage.py`
- `GeneralInstructionsPage.py`
- `SpecificationPage.py`
- `PowerConsumptionCalculationPage.py`
- `AdditionalInfoPage.py`

Это legacy-контур PDF-генерации, который все еще реально используется через backend-адаптеры.

## 3. Архитектурная картина

### 3.1. Общий стиль

Архитектурно проект ближе всего к модульному монолиту с выраженным слоением и примесью legacy-компонентов.

Новый backend уже организован по доменным модулям:

- `projects`
- `floor_plans`
- `equipment`
- `elements_geometry`
- `pipeline`
- `recognition`
- `signal_design`
- `documents`
- `shared`

Внутри модулей используются типичные слои:

- `application`
- `domain`
- `infrastructure`

Дополнительно есть более старый слой `backend/services/`, который пока не исчез, а частично инкапсулируется новыми use case-слоями.

### 3.2. Composition root и DI

Точка явной сборки зависимостей находится в `backend/composition.py`.
Там use case-объекты собираются из:

- SQLAlchemy-репозиториев;
- unit-of-work;
- event publisher;
- storage adapter;
- legacy orchestrator;
- PDF adapter.

FastAPI dependency injection оформлен в `backend/dependencies.py`.
Это означает, что роутеры не создают сервисы вручную, а получают уже собранные объекты через `Depends(...)`.

### 3.3. Текущее архитектурное состояние

Сильная сторона проекта: слоение уже есть, и оно достаточно осмысленное.

Главное архитектурное напряжение:

- часть системы уже модульная и современная;
- часть по-прежнему крупная, связная и опирается на legacy-функции.

Самые большие концентрации сложности:

- `floorplan-ui/src/pages/FloorPlanEditor.jsx` — около 12.5 тыс. строк;
- `backend/schemas.py` — около 1.3 тыс. строк;
- `backend/modules/shared/infrastructure/persistence/models.py` — около 1.1 тыс. строк;
- `backend/services/recognition_training_feedback_service.py` — около 1.1 тыс. строк;
- `backend/signal_planning.py` — около 1.5 тыс. строк.

Это не ломает систему прямо сейчас, но показывает области, где логика уже слишком плотно сконцентрирована.

## 4. Backend: устройство и ответственность

### 4.1. Точка входа

Backend поднимается через:

- `main.py` -> `uvicorn` -> `backend.app:app`

В `backend/app.py` происходит:

- создание FastAPI-приложения;
- инициализация БД;
- создание runtime-директорий;
- регистрация шрифтов PDF;
- установка middleware;
- подключение роутеров;
- публикация статики (`/uploads`, `/outputs`, `/debug_output`);
- настройка OpenAPI.

### 4.2. Роутеры

Основные роутеры:

- `projects_router`
- `equipment_router`
- `floor_plans_router`
- `elements_router`
- `pipeline_router`
- `recognition_router`
- `recognition_training_router`
- `pdf_router`

Роутеры в целом выглядят как тонкий слой входа в систему:

- принимают HTTP-запрос;
- валидируют payload через Pydantic;
- вызывают use case/service;
- возвращают DTO/response.

Это хорошая организация. Основная прикладная логика в основном вынесена ниже.

### 4.3. Shared-инфраструктура

Общая инфраструктура проекта включает:

- `RequestContextMiddleware` — request id и наблюдаемость;
- `SqlAlchemyUnitOfWork` — транзакционный контур;
- `SqlAlchemyEventPublisher` — аудит/события;
- `StorageService` и адаптеры — файловое хранение;
- `HealthService` — liveness/readiness.

### 4.4. Ошибки и API-поведение

В проекте есть единый обработчик доменных ошибок через `backend/errors.py`.
Ошибка отдается клиенту структурированно, в формате примерно вида:

- `code`
- `detail`

Это лучше, чем хаотичный набор необработанных исключений.

## 5. Frontend: устройство и ответственность

### 5.1. Точка входа и роутинг

Frontend находится в `floorplan-ui/` и построен на React 19.

Главный маршрутный слой находится в `floorplan-ui/src/App.jsx`.
Основные страницы:

- `/` — список проектов;
- `/create-project` — создание проекта;
- `/equipment` — каталог оборудования;
- `/projects/:projectId` — карточка проекта;
- `/floor-plans/:floorPlanId` — редактор плана;
- `/recognition-training` — страница дообучения.

### 5.2. API-доступ

Основной HTTP-клиент реализован в `floorplan-ui/src/api/client.js`.
Несмотря на наличие `axios` в зависимостях, реально основной клиент написан на `fetch`.

Там выделены namespace-объекты:

- `projectsApi`
- `equipmentApi`
- `floorPlansApi`
- `pipelineApi`
- `recognitionApi`
- `recognitionTrainingApi`
- `elementsApi`

Это хорошее решение, потому что API-вызовы централизованы и не размазаны по всем компонентам.

Есть и локальные исключения: например, `ProjectList.jsx` местами использует `fetch` напрямую, а не общий клиент.
Это уже маленькая архитектурная непоследовательность.

### 5.3. Главные frontend-модули

Самые значимые страницы:

- `ProjectDetail.jsx` — управление проектом, этажами, документами, оборудованием, генерацией PDF;
- `EquipmentCatalogPage.jsx` — каталог оборудования, загрузка изображений и PDF;
- `RecognitionTrainingPage.jsx` — управление обучающими примерами, runs и активацией моделей;
- `FloorPlanEditor.jsx` — центральный рабочий инструмент.

### 5.4. FloorPlanEditor

`FloorPlanEditor.jsx` — это ядро пользовательского сценария и одновременно главный технический узел риска.

Он совмещает в себе:

- визуализацию плана через Konva;
- зум, панорамирование, hit testing;
- редактирование стен, проемов, помещений, лестниц;
- работу с извещателями, приборами, трассами;
- orchestration pipeline шагов;
- сохранение на backend;
- предпросмотр связанных документов;
- побочный UI-state.

По сути это очень большой orchestration-компонент, где сосредоточены:

- представление;
- интерактивная логика;
- часть бизнес-правил;
- преобразование данных для API.

Это рабочее решение, но в перспективе его желательно дробить на hooks, canvas-слои и step-specific модули.

## 6. Технологии и используемые методы

### 6.1. Backend stack

Ключевые технологии backend:

- Python 3.12 как целевая версия для lint/format;
- FastAPI;
- SQLAlchemy;
- Alembic;
- Pydantic;
- Uvicorn;
- Pillow;
- OpenCV;
- OpenCV contrib;
- NumPy;
- Shapely;
- pytesseract;
- ReportLab;
- python-dotenv;
- Ultralytics.

### 6.2. Frontend stack

Ключевые технологии frontend:

- React 19.2;
- React Router DOM 6;
- React Konva;
- Konva;
- React Scripts / CRA toolchain;
- Testing Library;
- ESLint.

### 6.3. Методы распознавания и вычислений

В проекте используются несколько классов методов:

- классическое computer vision распознавание;
- OCR для извлечения размерных линий;
- геометрические преобразования и нормализация;
- расчет зон и покрытий;
- трассировка кабельных маршрутов;
- подготовка YOLO-датасетов и обучение через Ultralytics.

### 6.4. Методы в legacy-модуле `floorplan/`

Основной pipeline классического распознавания в `floorplan/main.py` и соседних модулях включает:

1. предобработку изображения;
2. детекцию стен;
3. поиск разрывов в стенах;
4. классификацию проемов;
5. удаление дублей;
6. детекцию помещений;
7. OCR размерных линий;
8. сбор debug-артефактов.

Это значит, что проект не опирается только на ML.
Сейчас он использует гибрид:

- классические CV-алгоритмы как основной baseline;
- retraining-контур как механизм повышения качества на отдельных шагах.

## 7. Хранение данных

### 7.1. Основная модель хранения

Проект использует смешанную модель хранения:

- реляционные данные в SQLite;
- бинарные и производные файлы на файловой системе;
- часть сложных структур — в JSON-полях БД.

### 7.2. База данных

По умолчанию используется:

- `floor_plans.db`

Подключение конфигурируется через `backend/config.py`.
При необходимости можно переопределить `DATABASE_URL`.

`backend/database.py` создает engine так:

- SQLite с `check_same_thread=False`;
- для других СУБД предусмотрен стандартный SQLAlchemy engine с `pool_pre_ping=True`.

### 7.3. ORM-структура

Главная ORM-модель сосредоточена в:

- `backend/modules/shared/infrastructure/persistence/models.py`

Ключевые сущности:

- `Project`
- `FloorPlan`
- `Wall`
- `Door`
- `Window`
- `Room`
- `Dimension`
- `FireAlarm`
- `SoueDevice`
- `ZkspcZone`
- `SignalInstrument`
- `CableRoute`
- `EquipmentItem`
- `ProjectEquipmentSelection`
- `ProjectEquipmentLink`
- `FloorplanRecognition`
- `PipelineDetectionSnapshot`
- `RecognitionFeedbackExample`
- `RecognitionTrainingBatch`
- `RecognitionTrainingBatchExample`
- `RecognitionTrainingRun`
- `RecognitionActiveModel`
- `AuditEvent`

### 7.4. Что именно хранится в БД

В БД хранятся:

- карточки проектов;
- этажи и их метаданные;
- геометрия и расположение объектов на плане;
- состояние pipeline по шагам;
- активный тип сигнальной подсистемы;
- каталог оборудования;
- привязка оборудования к проекту и к ролям;
- результаты распознавания;
- snapshots детектора;
- feedback для retraining;
- batches и training runs;
- активные модели;
- аудиторские события.

### 7.5. JSON-поля

Проект активно использует JSON-поля для гибких структур.
Например:

- `pipeline_state` у `floor_plans`;
- различные `*_overrides` у `projects`;
- `specs` у `equipment_items`;
- `recognition_result`;
- diff и snapshot-поля в retraining-контуре.

Плюс такого подхода:

- быстрое развитие схемы;
- возможность хранить сложные структуры без отдельной нормализации.

Минус:

- часть бизнес-данных становится менее строго типизированной на уровне БД;
- аналитика и сложные запросы становятся труднее;
- выше риск дрейфа схемы внутри JSON.

### 7.6. Файловое хранение

Основные runtime-директории:

- `uploads/`
- `outputs/`
- `debug_output/`

Что там лежит:

- исходные изображения планов;
- изображения оборудования;
- PDF-метки и инструкции по оборудованию;
- сгенерированные PDF-документы проекта;
- экспортированные retraining batches;
- датасеты и артефакты обучения;
- debug-изображения распознавания.

Backend монтирует эти директории как статические ресурсы, поэтому frontend может открывать связанные файлы через backend.

### 7.7. Сервис хранения

Файловые операции инкапсулированы в `StorageService`.
Он умеет:

- сохранять загруженные планы;
- сохранять изображения оборудования;
- сохранять PDF-этикетки и manuals;
- формировать debug-каталоги;
- перечислять debug-изображения.

Имена файлов формируются с учетом:

- сущности;
- id;
- номера этажа;
- timestamp.

Это снижает риск коллизий имен.

## 8. Миграции и эволюция схемы

### 8.1. Alembic

В проекте уже есть Alembic и baseline migration:

- `alembic/versions/20260316_000001_baseline_schema.py`

Она создает таблицы через `Base.metadata.create_all(...)` и создает ключевые индексы.

### 8.2. Особенность текущего состояния

Одновременно с этим `backend/database.py` при старте приложения делает:

- `Base.metadata.create_all(...)`;
- `_ensure_backward_compatible_columns()`;
- `_ensure_performance_indexes()`.

То есть проект сейчас не полностью опирается только на классический миграционный подход.
Он сочетает:

- baseline migration;
- автоматическое доведение legacy-баз до нужного состояния на старте.

Это удобно для разработки и обратной совместимости, но создает риски:

- схема развивается не только через Alembic;
- часть изменений скрыта в runtime-коде;
- повторяемость production-миграций хуже, чем при чистом migration-first подходе.

## 9. Как frontend общается с backend

### 9.1. Модель взаимодействия

Коммуникация построена как обычный REST over HTTP.

Frontend работает через относительные URL `/api/...`, а в `floorplan-ui/package.json` настроен proxy на:

- `http://127.0.0.1:8000`

Это позволяет в dev-режиме не хардкодить полный адрес backend.

### 9.2. Типы payload

Используются два основных формата:

- JSON для большинства CRUD- и orchestration-запросов;
- `multipart/form-data` для загрузки файлов.

JSON применяется для:

- проектов;
- оборудования;
- элементов плана;
- pipeline commit-операций;
- retraining endpoints;
- обновления проектных документов.

`multipart/form-data` применяется для:

- загрузки плана;
- загрузки изображения оборудования;
- загрузки label/manual PDF.

### 9.3. Поведение API-клиента

`apiRequest(...)` во frontend:

- делает `fetch`;
- проверяет `response.ok`;
- пытается вытащить `detail` или `code` из JSON-ошибки;
- поддерживает `rawResponse` для скачивания PDF;
- различает JSON и text response по `Content-Type`.

Это хороший базовый контракт для клиента.

### 9.4. Основные группы endpoint'ов

Главные API-направления:

- `/api/projects`
- `/api/floor-plans`
- `/api/equipment`
- `/api/walls`, `/api/doors`, `/api/windows`, `/api/rooms`, `/api/stairs`
- `/api/fire-alarms`, `/api/soue-devices`
- `/api/floor-plans/{id}/pipeline-*`
- `/api/floor-plans/{id}/process`
- `/api/recognition-training/*`
- `/api/projects/{id}/generate-pdf`
- `/api/projects/{id}/equipment-specification`
- `/api/projects/{id}/general-data`
- `/api/projects/{id}/general-instructions`
- `/api/projects/{id}/power-consumption-calculation`
- `/api/projects/{id}/additional-info`

### 9.5. Что важно в реальном потоке данных

Практический пользовательский поток выглядит так:

1. создается проект;
2. к проекту прикрепляются этажи и изображения планов;
3. frontend получает план и связанные элементы;
4. пользователь редактирует стену/проем/устройство;
5. frontend вызывает CRUD либо batch-save/commit endpoint;
6. backend сохраняет состояние и обновляет pipeline state;
7. frontend перерисовывает канвас по ответу API;
8. по завершении проектных шагов пользователь может сгенерировать PDF.

## 10. Pipeline распознавания и проектирования

### 10.1. Pipeline state

Состояние pipeline хранится в `PipelineStateModel`.
Есть базовые шаги:

- `walls`
- `openings`
- `rooms`
- `zkspc`

Есть branch-ориентированные шаги:

- `signal_instruments`
- `fire_alarms`
- `devices_cables`
- `soue_devices`
- `soue_cables`

Статусы шага:

- `draft`
- `validated`
- `stale`
- `locked`

У шага есть:

- revision;
- отметки времени;
- branch metadata.

Это важная часть архитектуры: система работает не как “свободный редактор”, а как редактор с управляемым жизненным циклом шагов.

### 10.2. Pipeline service

Главный orchestration legacy-сервис:

- `backend/services/pipeline_service.py`

Он:

- запускает детекцию стен;
- запускает детекцию проемов;
- запускает детекцию помещений;
- пересчитывает и фиксирует scale factor;
- пишет snapshots для retraining;
- синхронизирует данные шага с БД.

Через модульный слой это обернуто в `LegacyPipelineOrchestrator`, так что наружу backend уже выглядит чище, чем внутри.

### 10.3. Распознавание

Распознавание работает по гибридной схеме:

- если для шага есть активная retrained model, используется она;
- иначе используется legacy CV-алгоритм из `floorplan/`.

Это разумная переходная архитектура:

- старый пайплайн остается baseline;
- новые модели постепенно подменяют отдельные этапы.

## 11. Геометрия, сигнализация и инженерные расчеты

### 11.1. Геометрические элементы

Проект хранит и редактирует:

- стены;
- двери;
- окна;
- лестницы;
- помещения;
- размерные линии.

Для них есть:

- отдельные таблицы;
- Pydantic-схемы;
- CRUD endpoint'ы;
- frontend-инструменты редактирования.

### 11.2. Пожарные извещатели

Подсистема размещения пожарных извещателей реализована не только как CRUD.
Там есть и алгоритмический слой:

- расчет кандидатов размещения;
- проверка попадания в помещение;
- расчет покрытия;
- подбор точек.

Ключевые файлы:

- `backend/fire_alarm_placement.py`
- `backend/fire_alarm_cover_solver.py`

### 11.3. ZKSPC, приборы и кабели

Инженерная подсистема проектирования включает:

- расчет зон ЗКСПС;
- работу с сигнальными приборами;
- пересчет кабельных трасс;
- объединение маршрутов;
- типизацию по `system_type` и `subsystem_type`.

Ключевой вычислительный модуль:

- `backend/signal_planning.py`

Там сосредоточены:

- zoning;
- маршрутизация;
- numbering;
- анализ прохождения трасс;
- расчет длины;
- построение маршрутов для СПС и СОУЭ.

Это один из самых “инженерных” участков системы, где много не CRUD-логики, а вычислительных правил.

## 12. Каталог оборудования

Каталог оборудования — отдельный полноценный контур, а не справочник на пару полей.

Система умеет:

- создавать карточки оборудования;
- хранить категорию и характеристики;
- хранить `specs` в нормализованном JSON-формате;
- загружать изображение;
- загружать label PDF;
- загружать manual PDF;
- задавать совместимость между позициями;
- привязывать оборудование к ролям проекта.

Дополнительная особенность:

- на уровне проекта есть `project_equipment_selections` и `project_equipment_links`;
- на уровне генерации документов эти данные участвуют в спецификации и расчетах.

## 13. Документы и PDF

### 13.1. Модель

PDF-генерация в проекте не является побочным экспортом, а встроена как часть предметной области.

Через API можно:

- получить и редактировать спецификацию;
- получить и редактировать общие данные;
- получить и редактировать общие указания;
- получить и редактировать расчет токопотребления;
- получить и редактировать дополнительные сведения;
- сгенерировать финальный PDF.

### 13.2. Архитектура

Новый слой документов:

- `backend/modules/documents/application/use_cases.py`
- `backend/modules/documents/infrastructure/pdf_adapter.py`

Но реальная генерация делегируется legacy-коду:

- `backend/pdf_generator.py`
- root-level page/project classes

То есть здесь тоже реализована схема:

- современный backend use case layer снаружи;
- legacy генератор внутри.

### 13.3. Что попадает в PDF

По текущей логике в PDF-комплект могут входить:

- титульные листы;
- общие данные;
- общие указания;
- условные обозначения;
- чертежи по этажам;
- разделы ЗКСПС, СПС, СОУЭ;
- спецификация оборудования;
- расчет токопотребления;
- дополнительные сведения.

Шрифты регистрируются из:

- `GOST_A.TTF`
- `GOST_A_Bold.ttf`

## 14. Retraining и ML-контур

### 14.1. Общая идея

В проект встроен полный контур дообучения распознавания, а не просто “кнопка сохранить feedback”.

Сценарий такой:

1. пользователь исправляет результат распознавания;
2. система сохраняет feedback example;
3. оператор курирует примеры;
4. из approved examples собирается batch;
5. по batch запускается training run в subprocess;
6. датасет и артефакты сохраняются на диск;
7. run можно активировать как текущую модель для шага.

### 14.2. Ключевые сущности

Retraining-контур разделен на три сущности:

- example;
- batch;
- run.

Это очень хорошее архитектурное решение, потому что:

- отделяет исходные данные от их экспортированного среза;
- обеспечивает воспроизводимость;
- позволяет переиспользовать один example в нескольких batches;
- сохраняет историю.

### 14.3. Основные backend-компоненты

Ключевые файлы:

- `backend/services/recognition_training_feedback_service.py`
- `backend/services/recognition_training_management_service.py`
- `backend/routers/recognition_training.py`
- `tools/run_recognition_training.py`

### 14.4. Хранение retraining-данных

На уровне БД хранятся:

- detection snapshots;
- feedback examples;
- training batches;
- связи batch-example;
- training runs;
- active models.

На диске сохраняются:

- `outputs/recognition_feedback/<batch_id>`
- `outputs/recognition_training/<run_id>`

Внутри лежат:

- экспорт датасета;
- manifest;
- конфиг запуска;
- логи;
- веса модели;
- метрики.

### 14.5. Модель запуска

Training запускается отдельным subprocess через:

- `python tools/run_recognition_training.py --run-id <run_id>`

Это правильное решение, потому что:

- FastAPI-запрос не должен жить столько, сколько идет обучение;
- тяжелая задача не должна блокировать web-процесс;
- аварии обучения не должны валить API-сервер.

### 14.6. Место ML в текущей системе

ML-контур встроен аккуратно: он не заменяет весь backend, а усиливает отдельные шаги pipeline.
Сейчас это выглядит как зрелый экспериментальный production-ready контур, но не как полностью автономная MLOps-платформа.

## 15. Тестирование

### 15.1. Python тесты

В проекте настроен `pytest`.
Конфигурация:

- `pytest.ini`
- `.coveragerc`

Покрываются:

- backend API и integration-сценарии;
- `floorplan` модуль распознавания.

Тестовая инфраструктура в `tests/conftest.py`:

- поднимает отдельный uvicorn-процесс;
- использует временную SQLite-базу;
- ждет готовности API;
- изолирует окружение тестов от рабочей БД.

Это хороший признак зрелости тестового контура.

### 15.2. Frontend тесты

Во frontend используются:

- React Testing Library;
- Jest через `react-scripts`;
- ESLint с правилами для тестов.

Покрываются страницы и важные модули:

- `App`
- `ProjectDetail`
- `FloorPlanEditor`
- `EquipmentCatalogPage`
- `RecognitionTrainingPage`
- утилиты геометрии;
- отдельные editor hooks/sections.

### 15.3. Фактический статус проверок на момент среза

Были выполнены реальные проверки.

#### Backend

Команда:

- `python -m pytest`

Результат:

- `192 passed`
- `1 skipped`
- `1 warning`

Вывод: backend и Python-часть проекта находятся в хорошем рабочем состоянии.

#### Frontend build

Команда:

- `npm run build`

Результат:

- сборка успешна;
- есть warnings, но production bundle собирается.

#### Frontend lint

Команда:

- `npm run lint`

Результат:

- `76 problems`
- `43 errors`
- `33 warnings`

Основные причины:

- нарушения правил Testing Library в тестах;
- warnings и code-smells в `FloorPlanEditor.jsx` и `floorPlanGeometry.js`;
- неиспользуемые переменные;
- проблемы с hook dependencies;
- unreachable code.

То есть lint-контур сейчас не зеленый.

#### Frontend tests

Команда:

- `npm test -- --watch=false --runInBand`

Результат:

- `83 passed`
- `3 skipped`
- `1 failed suite`
- `15 passed suites`
- `16 total`

Единственный фактически упавший блок:

- `src/pages/floorPlanEditor/useSignalBranchState.test.jsx`

Суть падения:

- тест ожидает `active_signal_system_type = non_addressable`;
- фактическое состояние после обновления ветки показывает `addressable`.

Это не выглядит как тотальная поломка фронтенда, но указывает на реальное несоответствие логики branch state и ожиданий теста.

### 15.4. Общая оценка качества тестирования

Сильные стороны:

- backend покрыт хорошо;
- есть интеграционные тесты не только на функции, но и на API;
- есть frontend unit/integration coverage;
- распознавание тестируется отдельно.

Слабые стороны:

- frontend quality gate не проходит полностью;
- lint не чистый;
- один из editor-state тестов падает;
- в самых крупных файлах риск регрессий выше из-за высокой связности.

## 16. Наблюдаемость и эксплуатационные аспекты

В проекте уже есть базовые эксплуатационные признаки:

- `health/live`
- `health/ready`
- request context middleware;
- логирование;
- аудит-события;
- runtime-директории создаются автоматически;
- шрифты регистрируются на старте.

Но это еще не полноценная production-операционная платформа.
На текущем этапе это скорее хорошо организованный прикладной контур разработки и локального/стендового запуска.

## 17. Сильные стороны проекта

Главные сильные стороны:

- предметная глубина: проект решает реальную инженерную задачу, а не только базовый CRUD;
- наличие модульной backend-архитектуры;
- понятный REST API;
- развитая модель данных;
- интеграция с PDF-генерацией;
- полноценный retraining-контур;
- сильное backend-тестирование;
- аккуратная файловая организация runtime-артефактов;
- явный composition root и dependency wiring.

## 18. Технические риски и ограничения

### 18.1. Большие файлы и высокая связность

Основной риск — слишком крупные модули:

- `FloorPlanEditor.jsx`
- `signal_planning.py`
- крупные ORM/schema/service файлы

Это затрудняет:

- локализацию багов;
- изменение логики без побочных эффектов;
- поддержку несколькими разработчиками;
- точечное покрытие тестами.

### 18.2. Смешение modern и legacy

Проект уже идет в сторону модульного backend, но часть критической логики still lives в legacy-слое.
Это не плохо само по себе, но означает:

- две архитектурные эпохи одновременно;
- более сложный onboarding;
- более высокую цену рефакторинга.

### 18.3. Эволюция схемы БД

Сейчас схема развивается и через Alembic, и через runtime self-healing в `database.py`.
Для разработки это удобно, но для строгого production-режима это спорное решение.

### 18.4. Frontend quality gate

Frontend собирается, но не проходит полностью:

- lint;
- часть тестов.

Это означает, что доставка фронтенд-изменений сейчас менее надежна, чем доставка backend-изменений.

### 18.5. SQLite как база по умолчанию

Для локального сценария SQLite подходит хорошо.
Но при росте проекта могут появиться ограничения:

- конкурентная запись;
- сложные аналитические запросы;
- эксплуатационные сценарии многопользовательской работы.

## 19. Практический вывод

Проект уже представляет собой крупную прикладную систему, а не прототип.

По зрелости он выглядит так:

- backend: зрелый и структурно сильный;
- предметная логика: глубокая и насыщенная;
- frontend: функционально богатый, но архитектурно перегруженный в центральном редакторе;
- хранение: смешанное, практичное, но местами уже нуждается в более строгой дисциплине схемы;
- тестирование: хорошее в backend и неполностью стабилизированное во frontend;
- retraining и распознавание: одна из самых интересных и сильных сторон проекта.

Если кратко, то это уже рабочая инженерная платформа с хорошим потенциалом развития, но с явными зонами технического долга:

- сверхкрупный frontend-редактор;
- гибрид modern/legacy;
- не до конца вычищенный frontend-контур качества;
- не полностью миграционно-центричная эволюция БД.

## 20. Что стоит сделать дальше в первую очередь

Если рассматривать следующие технические приоритеты, я бы выделил такие:

1. Разбить `FloorPlanEditor.jsx` на отдельные hooks, canvas-слои и orchestration-компоненты.
2. Довести frontend до зеленого состояния по `lint` и `test`.
3. Постепенно выносить legacy-логику PDF и pipeline за более четкие adapter boundaries.
4. Определиться, будет ли схема БД дальше жить через Alembic-first подход.
5. Разделить крупные backend schema/model/service файлы по доменным областям.

---

Если нужен следующий шаг, на базе этого отчета можно отдельно подготовить еще 3 вида материалов:

- краткую версию для защиты диплома;
- архитектурную схему по слоям и потокам данных;
- таблицу “модуль -> ответственность -> данные -> API -> риски”.
